# 题意解析与建模衔接备忘录

## 工作边界与依据

- 本轮仅阅读、题意解析和数学建模审查，不编写求解代码、不求解、不撰写论文、不生成结果工作簿。
- 已完整读取 `question/C题(1).pdf` 三页，并核对原页；已完整读取 `Pr/问题一_数学建模与Agent实现规范.md`。
- 原规范作为问题一基础框架保留；本文件补充其边界与待决事项，不静默替换其中的假设。
- 尚未读取附件工作簿，因此数据采样间隔、附件1具体日期、列结构和模板结构均未核实。
- 区分题面硬约束、数学推论和自设假设；后续不得把暂定口径写成题面事实。

## 题目背景与任务分层

系统包含小区负载、光伏、储能及外部电网。储能通过跨时段转移电量降低购电费，但受到效率损耗、容量和功率限制。核心是供电满足负载条件下的跨时段采购与储能联合优化；最少购电量不等于最低购电费。

| 问题 | 新增条件 | 建模重点 |
|---|---|---|
| 1 | 重复的日内电价与负载曲线、一天光伏预测、日首日末储电量相等 | 以预测作为确定性输入的日前调度；可行性保证针对该预测输入 |
| 2 | 实际负载和光伏变化、紧急购电按当时电价5倍计费 | 日前可用信息、预测误差、计划与实际执行分离 |
| 3 | 0/6/12/18时发布未来24小时整点光伏预测；减少计划部分50%违约电价，增加部分1.5倍电价 | 非前视滚动决策、交易结算定义、预测更新价值 |
| 4 | 实时波动电价，重算问题2和3 | 电价何时可知、预测与实际结算的区别 |

“每天相同”不表示一天内各时刻相同。问题1保持时间变化的电价和负载曲线。

## 问题一继承的数学骨架

令 t=0,...,143，时间步长 Δt=1/6 小时，E_0,...,E_144为边界储电量。x_t为计划购电量，q_ch,t和q_dis,t为母线侧充放电量，r_t为弃光量，均以kWh计；E_t为电池内部电量。

若输入是时段平均功率，则D_t=L_tΔt，G_t=P_PV,tΔt；若输入为时点功率，必须先定义区间积分近似，不能默认直接除以6。

主目标：min Σ_t c_t x_t。

约束：

- 能量平衡：x_t+G_t-r_t+q_dis,t=D_t+q_ch,t。
- 状态递推：E_(t+1)=E_t+η_c q_ch,t-q_dis,t/η_d。
- 储电量：1200≤E_t≤10800，覆盖全部145个边界。
- 母线侧功率上限暂定：0≤q_ch,t,q_dis,t≤5000/6；计算时保留精确值，833.3333仅为展示近似。
- x_t≥0，0≤r_t≤G_t。
- 问题1日内循环：E_144=E_0。
- 初始状态应显式写为E_0=e_init；e_init依据待核实。若改成可优化变量，必须明确这是典型日循环状态联合优化，不能与给定当前电量的运行调度混称。

暂按原规范η_c=η_d=0.9，此时往返效率是0.81。若90%指往返效率，应要求η_cη_d=0.9；对称拆分为√0.9是额外假设。

主目标不加入紧急费、调整费、电池折旧费或售电收益。允许弃光、暂不考虑售电以及功率上限的母线侧解释均需标为模型口径。

## 需要补充或修正的建模边界

### 1. 初始电量不能由缺少日期自动推成自由变量

题面只明确2025-01-01 0:00为6000kWh，附件1说明为“某天”。附件1日期未知时，保留e_init参数并分析其影响。若优化E_0，相当于同时选择起始状态，可能产生不可直接执行的成本优势；该方案只适合作为另一个明确命名的模型或界限比较。

### 2. 同时充放电与次级目标必须联合处理

正电价并不保证每个LP最优解都不同时充放电，尤其在光伏富余、弃光无成本的退化时段。

例：取q_ch=a>0、q_dis=η_cη_d a，则状态变化为零，但净吸收母线能量(1-η_cη_d)a。模型可借此降低弃光量且不改变购电费。故原规范中先最小弃光、再最小吞吐量的顺序可能主动偏好这种损耗循环，第三层不能保证消除它。

建议：保留成本LP作为基线并检查解；如果以最小弃光作为次级目标，则加入q_ch≤zQmax、q_dis≤(1-z)Qmax、z∈{0,1}作为可执行储能调度约束。该约束属于物理建模选择，不是题面逐字条件。若继续使用纯LP，优先研究同成本最小吞吐量的解选择并复核互斥性，不把它等同于已证明的设备级执行保证。

### 3. 时段能量可行性不自动保证瞬时功率可行性

10分钟输出要求确定的是购电量颗粒度；原始功率如何覆盖区间尚待核实。采用分段常功率和互斥模式时，状态在段内单调，检查边界容量即可约束段内容量；存在段内变化时，需要额外验证或细化调度假设。

### 4. 日内循环不应直接复制到问题2—4

问题1明确E_144=E_0。问题2—4应首先保证E_(d+1,0)=E_(d,144)，不应每天重新初始化6000或不加解释地强制每日回到初值。为避免滚动模型透支末端电量，可设计终端储能目标或残值，但必须单列为策略假设。

### 5. 未来实测数据与决策可用信息分开

问题2当天0:00的计划不能直接利用当天尚未发生的实测负载和光伏；这类全知结果只能作为事后理想基准。问题3只能使用决策时刻已经发布的预报，已经执行的区间不允许修改。问题4不能因附件4提供全年实际电价，就默认0:00知道全天价格。

附件数据覆盖2025年全年，但问题2—4结果范围是2025-02-01至12-31。1月可作为历史预测建模期，但这是合理用途而非题面指定事实；2月1日储电量须通过从1月1日初始条件出发的状态演化或明确的初始化假设衔接。

### 6. 问题3结算条款需要先定义再写总成本

至少区分原计划x_t、调整后总量y_t、增加量(y_t-x_t)_+、减少量(x_t-y_t)_+和紧急量。题面未详细展开减少计划部分原电费是否退回、重复调整按原计划还是上次计划结算等规则。不得默认为任一种口径；应列明解释并检查零调整、纯增加、纯减少、重复调整案例，防止费用重复计算。实际供电平衡使用最终交付量，不把计划量和调整后总量重复相加。

## 建模阶段可推导的校验与解释

在问题1日内循环和固定效率下：

Σq_dis=η_cη_dΣq_ch。

因此：

Σx=ΣD-ΣG+Σr+(1-η_cη_d)Σq_ch。

它用于独立核对全日电量和损耗，并解释储能套利可能增加购电量但降低购电费。

忽略容量竞争、功率约束和其他机会成本时，以外网低价电充电供高价时段使用的必要经济比较是c_high>c_low/(η_cη_d)。本基线对应c_high>c_low/0.81；这是局部解释，不是全局最优策略。

## 后续代码阶段必须满足的要求（本轮不实现）

1. 先核验附件1—5的日期、字段、单位、时间采样含义和模板，不读取Excel的~$锁文件作为数据。
2. 区分144个交易区间与145个状态边界；0:00+1是24:00，不新增第145笔日内交易。
3. 重采样方法须匹配时点值/时段均值含义，并检查积分电量；阶梯电价不随意线性插值。
4. 为效率、功率所在侧、初始状态、终端条件及费用解释建立统一参数口径。
5. 保存输入、决策可用信息时间戳、计划值、实际执行值与求解状态；预测训练与回测按时间先后划分。
6. 独立复算逐段能量平衡、SOC递推、全部边界、功率、非负性、互斥性、日末条件、全日电量损耗恒等式及费用；记录容差和MILP最优性间隙（如适用）。
7. 同成本次级优化设置明确成本容差，并报告其对主成本的实际影响。
8. 输出表从完整轨迹自动汇总，明确充放电量是母线侧还是电池侧，不用净SOC变化代替充放电总量。
9. 问题1输出result1.xlsx；后续分别为result2.xlsx、result3.xlsx、result4-2.xlsx、result4-3.xlsx。
10. 问题1保留六个指定10分钟购电量、全天电量/费用、六个4小时充放电汇总及首末储电量；问题2—4保留全年要求范围及3月20日、6月21日、9月23日、12月21日的重点结果。

## 后续论文与结果解释阶段必须保留的要求（本轮不撰写）

- 明确题面约束与自设假设，尤其效率、初始电量、弃光、售电边界、功率侧、互斥和结算。
- 解释低价充电、高价放电是否发生及为何发生，不将其作为先验固定规则。
- 用同一初始状态和信息条件比较无储能/简单调度基线；全知结果单列为理想基准。
- 检查效率解释、初始储电量和终端策略等敏感性；不虚构已验证结果。
- 问题3以相同实际数据和初始条件比较0点预测与多次更新方案，分解计划、调整、违约和紧急费用后判断更新价值。
- 论文数值、图表和结果工作簿均源自同一经校验轨迹，保留可复核依据。

## 问题二建模补充与衔接要求

已新增《[问题二_数学建模与Agent实现规范](问题二_数学建模与Agent实现规范.md)》，按问题一的22节框架编排。该文件为数学模型和后续实施约定，尚未读取附件工作簿、求解或验证数值结果。

### 继承与新增

- 继承144个10分钟交易区间、145个状态边界、母线侧充放电、电池内部状态、暂定各90%效率、容量1200—10800和功率5000kW口径。
- 增加固定日前计划x、紧急量u、未接收但仍付费的计划量s；实际普通接收量为x-s。
- 实际平衡改为x-s+u+G-r+q_dis=D+q_ch；实际费用为Σcx+5Σcu，不按x-s收费，不把终端价值算成账单。
- s的存在依赖“允许物理未接收但计划不退款”的自设解释；需核验过剩交付机制，不能用弃光r掩盖计划过剩。

### 必须冻结的信息与策略约束

1. 日前只用当日0:00前已有历史，问题二不提前使用附件3。实际数据随时间揭示，计划发布后冻结。
2. 主模型为条件场景下的因果调度，有限场景树需保留逐步分支及共享历史动作；独立整日场景的全知补救不能冒充可执行策略。
3. 日内可依据已观测数据调整储能及紧急补购，不修改x；实际运行滚动求解只执行当前动作，记录其与日前场景模型之间的近似差异。
4. 最终运行模型采用充放电互斥；额外规定紧急购电时不充电、不弃光、不拒收计划，避免终端价值诱导紧急电充电。这些模式是自设策略限制。
5. 跨日继承真实SOC，不继承预测均值或每日重置6000。1月1日6000是题面初值，2月1日初值来自透明预热过程或另设情景。
6. 终端未来成本V单列：V=0为短视对照，非零参数只用过去数据校准。不得把每日回到初值当成问题二硬要求。
7. 当前10分钟内可响应、分段常功率是离散运行假设，不把事后区间均值误称为段首已知数据。

### 后续实现新增验收

- 读取附件1、2和result2模板后再冻结数据格式；预处理与预测全链路检查时间泄漏。
- 验证未接收边界、紧急缺口公式、两种运行模式及计划不可改写；实际负载超出训练场景时不能截断数据。
- 非循环日能量恒等式为：Σ(x-s+u)=Σ(D-G+r)+(1-η_cη_d)Σq_ch+η_d(E_144-E_0)。
- 区分计划电量Σx、紧急电量Σu、总付费电量Σ(x+u)及实际接收Σ(x-s+u)，各自标签清晰。
- 输出2025-02-01至12-31共334天，预热期另存；四个指定日按题面展示。
- result2计划表只填x，储能表填实际轨迹，紧急表填实际u；合并相邻紧急区间后仍按原时段电价复算费用。
- 核验表1的购电量展示口径，计划/紧急/总量不能无标签混填。
- 对照和敏感性保留效率、场景结构、终端价值、未接收机制及冷启动影响；全知基准只在可比条件下讨论理想界限。

### 后续论文新增要求

说明过量计划与5倍缺口费用的权衡、信息可用时点、预测/实际差别、跨日状态和终端项；报告实际费用分解、紧急量、未接收量及剩余储电量。所有结果必须来自因果闭环运行，不能用日前预期成本代替实测费用。

## 问题三建模补充与衔接要求

已新增《[问题三_数学建模与Agent实现规范](问题三_数学建模与Agent实现规范.md)》，继续采用问题一的22节结构，继承问题二物理约束、因果执行和跨日状态。尚未读取工作簿、求解或形成数值结论。

### 变量与事件关系

- x是0点原计划且永久保留；y^(k)是第k次发布后的合同总量，不是增量；δ是相对上版变化；A正负部分是最终合同相对原x的偏差。
- 0/6/12/18时对应索引0/36/72/108；每轮更新当天剩余区间，下一轮前生效的版本构成唯一最终合同y_bar。
- 物理平衡为y_bar-s+u+G-r+q_dis=D+q_ch；s扩大为未接收合同量，0≤s≤y_bar，不能仍限于原x。
- 预报发布时接收新版本并调整合同，逐10分钟观测只调整物理动作；已经执行的合同、SOC、费用不能回写。
- 使用未来24小时预报不等于提前正式签订次日合同；当前时域截到当天24点，跨日状态仍取真实末电量。

### 结算口径与冲突必须显式保留

1. 暂定主模型R：取消部分退原款，再付50%违约费；最终普通合同费B_R(x,y)=cx-0.5c(x-y)_++1.5c(y-x)_+=cy+0.5c|y-x|。
2. 对照N：原款不退且减少部分另收50%，B_N=cx+0.5c(x-y)_++1.5c(y-x)_+。R不是题面明确承诺的退款规则，须后续核验。
3. 沿用免费物理未接收机制时，N下主动减少原计划可能被保留合同后未接收支配；这一冲突不得隐藏或误报成普遍市场结论。
4. 暂按最后有效版本相对原x结算一次；中间版本是可替换修订。可审计B(x,y_new)-B(x,y_old)，不累计各版本全额偏差费。
5. 若实际规则为每次调整不可撤销成交，必须重建逐笔合同/退款账本，不能沿用最终净偏差公式。
6. 实际费用为ΣB_R+5Σcu，终端V不入账；s不产生退款，不能与合同取消量A^-混同。
7. “交易时刻”暂按交付时段电价处理，生效延迟暂取零；两者需核验，问题四尤其不能直接替换价格数组。

### 信息与预报处理新增验收

- 附件3须保存发布时间和有效时间双索引；转换整点功率时只在当时已发布版本内处理，不能取整个文件最新版本回填过去。
- 合同决策可见新预报和此前已发生实测，不能先看随后10分钟事后均值再修改合同；物理层沿用当前段响应近似。
- 0点多阶段场景若考虑未来调整，必须模拟未来预报发布与实测联合变化，不能读取实际未来预报版本。
- 同一发布节点共享合同决策，发布间隔内不能让合同随每个观测场景变化；实际物理动作可以因果变化。
- 小时预报转10分钟须核验首末点和积分口径；缺失端点不能用尚未发布版本补齐。未来24小时端点和次日部分单独处理。
- 未来预报联合分布不足时可用当前版预测滚动基线，但须标注未完整计算未来信息价值，不宣称精确多阶段最优。

### 账务、输出与结果评价新增要求

- 必测c=1、x=100时y=100/120/80/0分别费用100/130/90/50；版本100→120→80最终90，100→80→100最终100。这些只在R及最终结算假设下成立。
- 原计划表填x，调整表核验是总量y还是差额，储能/紧急表填实际轨迹；另存所有真实发布版本和最终y_bar，不输出场景中尚未发生的版本。
- 复核Σ(y_bar-s+u)=Σ(D-G+r)+(1-η_cη_d)Σq_ch+η_d(E_144-E_0)，费用用原计划/退款/违约/新增/紧急五项独立复算。
- 实际合同、计划、紧急、接收量分别标注；存在退款和违约时不再用一个“总付费电量”代替费用解释。
- 评价采用S0（0点预报、无调整）、SI（新预报仅调储能）、SA（无新预报但允许按已有信息调合同）、SF（新预报与调整均用），区分信息和交易机会。
- 比较6/12/18更新子集时说明禁止的是读取信息、调整合同还是两者；选择规则在过去数据上确定，事后最便宜子集只作回顾。
- 各方案共同起始状态、实际输入和结算规则，连续演化并报告末端SOC；问题二与三的差异还包含0点预报来源，不能全归因于日内更新。
- 附件未给出的新增预报时刻只能做假设条件分析，不伪造实测预报或收益。无结果前不宣称每次更新必然节费。

## 问题四建模补充与衔接要求

已新增《[问题四_数学建模与Agent实现规范](问题四_数学建模与Agent实现规范.md)》，采用问题一的22节结构，分别建立4-2固定计划和4-3滚动调整分支。本轮未读取工作簿、编写代码、求解或生成结果表。

### 价格信息和交易口径

1. 主信息口径U：未来实际电价未知，仅用已经公布价格和历史预测。全天提前已知K需发布时间依据，否则只作电价完全信息基准。
2. 主结算口径S：计划确定量但不锁价，普通/调整/退款/违约均用交付区间实际价，紧急用5倍同价。该假设须核验，不把预测价当成交价。
3. U/K信息口径和S/锁价结算是独立维度；附件4有全年数据并不能证明任一时刻可查看未来。
4. 当前价暂设区间起点可见，若价格事后公布，当前运行也不能使用当段实际价；明确事件顺序和延迟。
5. 4-3延续R退款、最终偏差一次结算，另留N/逐笔替代。若签约价各异，须明确原价、增购价、退款价、违约基础价，不能只替换c数组。
6. 4-2主比较沿用问题二历史光伏预测；题目四整体列出附件3，故保留4-2-PV0对照并核验正式输出使用范围，不悄然增加信息。

### 费用和物理模型接口

- 定义h=x（4-2）或h=y_bar（4-3），统一平衡h-s+u+G-r+q_dis=D+q_ch，未接收0≤s≤h，真实SOC跨日连续。
- 4-2实际费Σc_actual x+5Σc_actual u；4-3主费Σ[c_actual y_bar+0.5c_actual|y_bar-x|]+5Σc_actual u。
- 未来目标采用负载—光伏—价格联合条件场景，E[c u]一般不等于E[c]E[u]；当前已确定x可以提出条件期望之外。
- 每轮价格预测改变只更新预期，不产生真实冲回现金流；最终一次以实际价复算，不能累计不同预测价下的版本账单差。
- 电价刷新不增加交易权限：4-2日内x固定，4-3只在约定发布节点调合同。
- 终端V、场景期望和真实账单分开；各分支独立推进实际SOC，不共享可变状态。

### 价格符号与数据新增验收

- 读取附件4后检查价格单位、时间语义、可见时点、零/负值和尖峰；不能为满足模型而裁剪负价或删除尖峰。
- 正价是绝对值上图费用和经济交易上界的条件；零价需独立复算辅助变量，负价需精确分段表达并重审模型有界性。
- 负价加无限计划和免费未接收可能导致无界获利；不得把正价需求上界继续宣称为无损等价限制。须核验真实合同约束或明确另设情景。
- 价格预测、变换、缺失填补、调参按时间隔离；场景须保留价格与缺口相关性，未来价格不能通过场景标识泄漏。
- 时间细分/平均价计费须有区间成交假设；kWh变量不再次乘时长，价格不作为电量处理。

### 验证、输出及后续评价

- 4-2算例c=(1,2)、x=(10,10)、u=(0,1)费用40元；4-3 R算例c=2、x=100、y=80、u=3费用210元。
- 4-3版本100→120→80在实际价2下普通合同费180元，不受中途预测价1或3影响；N口径普通费220元，均另计紧急费。
- 恢复重复已知价格且保持同一光伏信息和终端设置时，两分支应退化回问题二/三；仅换价格数组不保证相同信息条件。
- 输出result4-2.xlsx和result4-3.xlsx，继承334天、四个指定日及各模板字段要求，原计划/调整/紧急/接收量各自标注。
- 同一波动价格下比较原策略闭环、因果点预测、联合场景策略与标注清楚的理想价对照；原固定价成本与波动价成本之差不能直接叫算法节省。
- 固定轨迹只换价属于纯重定价诊断，不是重新优化；分离环境价格变化、光伏信息增加和合同调整的价值。
- 对照统一起始状态与末端评价，报告费用分项、紧急、未接收、弃光、期末SOC和价格预测限制，不虚构节费结论。

## 四问后续代码书写与使用建议

已补充《[四问代码实现路线与使用指南](四问代码实现路线与使用指南.md)》，覆盖架构、各问代码路线、工具利弊、验证、性能与拟议CLI用法。该文件是实现建议，不代表代码、命令入口、依赖或许可已就绪。

- 推荐Python + Pyomo + HiGHS作统一主线；SciPy可做问题一小型独立核验；Gurobi仅在许可可用和同模型实测后选择。
- 统一物理约束、合同/账单接口与因果信息视图，各问只切换信息、交易、价格和终端规则，避免四份独立复制程序。
- 开发顺序为数据审计/手算校验→问题一→问题二点预测闭环→问题三合同版本与账务→问题四价格层→必要的联合场景升级。点预测滚动是明确近似，不替代完整场景规范。
- 负载/光伏/价格预测优先可解释基线，附件3按发布版本利用；所有训练、特征和误差样本按实际可见时间隔离。
- 逐10分钟全年优化每分支约48,096次，先测日/周耗时；检查点记录实际SOC、配置和随机状态。连续日期不能简单并行重置，独立实验可以并行。
- 保留能量、费用、发布边界、未来数据替换、跨日续跑与模型退化用例；导出前回读模板结果与实际轨迹核对。
- 正式结果来自已验证的实际轨迹。超时可行、最优、回退分开标注，不能返回零数组掩盖失败。
- 拟议目录和CLI命令尚未实现；未来README只能写实测环境与运行时间，不能把设计示例当现成使用说明。

## 下一阶段入口

四问的建模规范已建立。下一阶段先只读核验附件1—4及附件5全部模板，统一时间/单位、效率、功率侧和跨日口径；优先确定价格发布时间与结算、取消退款、重复调整、未接收机制及4-2光伏信息范围。只有这些假设明确后再冻结具体数据模型；未经用户要求，不开始写求解代码或论文。
