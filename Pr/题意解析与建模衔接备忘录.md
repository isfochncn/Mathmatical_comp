# 题意解析与建模衔接备忘录

> 2026-09-10按用户逐项决定更新。本文件与四问规范共同约束后续实现和论文；当前仅建模。旧检验报告作为历史审查留存，其已被确认事项不得继续作为主模型开关。

## 1. 已冻结的高影响口径

本次追加确认优先于此前“预付策略批次”解释。已冻结如下：

| 事项 | 有效口径 | 范围 |
|---|---|---|
| 10分钟标签 | 00:10是00:00—00:10的平均价格/功率，24:00是当天最后10分钟 | 全部 |
| 小时预报 | 预报j小时是第j个未来小时的平均功率，无需补0小时或插值 | 三、4-2、4-3 |
| 有效充放电功率 | 两向5000×0.9=4500kW；每10分钟实际有效电量上限750kWh | 全部 |
| 购电对象 | 实时策略及实际执行，不引入策略批次、预付持仓和批次旧价 | 三、4-3 |
| 最终购电量 | 只统计当日实际执行量，放弃的中间方案量不累计 | 全部 |
| 最终费用 | 实际执行电费加已发生违约金；违约按减少计划时的当前价计 | 三、4-3 |
| 费率 | 普通1、实际调整增购1.5、紧急5；减少量违约0.5，分别乘事件当时价 | 相应分支 |
| 无拒收 | 二/4-2日计划固定，多买须付费并使用/保存，不足可紧急补购 | 二、4-2 |
| 信息 | 未来实际量未知，已发布预报和当前观测可用；当前功率不等于未来区间均值 | 二至四 |
| 跨日 | 实际末SOC、历史与跨日预测进入次日决策 | 二至四 |
| 初始与效率 | 初始6000，两向效率各0.9；只有问题一日首末相等 | 全部 |

以上区分用户确认解释与题面事实，不声称是官方新说明。旧“退原成交金额”的批次模型已被最新实时策略解释取代，不再列作主模型。

## 2. 四问框架与权限

| 分支 | 0点及未来信息 | 普通策略 | 日末条件 |
|---|---|---|---|
| 问题一 | 给定日内电价/负载及全天光伏预测 | 0点制定当日计划 | 初末均6000 |
| 问题二 | 重复价格曲线、历史预测；不提前读实际负载/光伏 | 全天固定，仅不足时紧急加购；无拒收 | 实际传次日 |
| 问题三 | 已发布0/6/12/18光伏预报、历史负载预测 | 指定节点逐笔增减尚未交付策略 | 实际传次日 |
| 4-2 | 已发布光伏预报，历史及当前真实价形成预测 | 全天固定，不因预测更新重签 | 实际传次日 |
| 4-3 | 同上，结合最新预报和价预测 | 指定节点逐笔调整 | 实际传次日 |

四份规范均采用22节结构：目标、时间、变量、单位、费用、平衡、状态、功率容量、边界、信息、额外限制、次级目标、完整模型、数据、代码要求、校验、输出、论文要求、技术未决、衔接、顺序、原则。

## 3. 统一物理核

四问均取 \(\eta_c=\eta_d=0.9\)。为准确表示用户确认的“实际4500kW”，本次统一变量定义：

- \(q^{ch}\)：效率损耗后实际存入电池的电量；
- \(q^{dis}\)：效率损耗后实际送达微网的电量；
- 微网充电输入为 \(q^{ch}/0.9\)，电池放电消耗为 \(q^{dis}/0.9\)。

于是：
\[
E_{d,t+1}=E_{d,t}+q^{ch}_{d,t}-q^{dis}_{d,t}/0.9.
\]
充电效率体现在供需平衡的 \(q^{ch}/0.9\) 中，不再对已存入的qch乘一次0.9。旧稿qch定义为母线输入量，旧公式不能与本版变量混用。往返效率仍为0.81。

只有实际能量交换改变E，修改未来计划不直接改变储电量。

\[
1200\le E_{d,t}\le10800,\qquad
P_{\max}^{effective}=5000\times0.9=4500\ {\rm kW},
\]
\[
0\le q^{ch}_{d,t},q^{dis}_{d,t}\le4500/6=750\ {\rm kWh}.
\]
4500的单位是kW，不是W。充电有效存入功率与放电有效输出功率均以4500kW为上限；损耗前的相应输入/消耗功率为有效功率除以0.9，至多5000kW。不得将有效750kWh再乘0.9作为实际充电上限。

额定容量12000不替代允许运行上限10800。储能功率不是外网购电上限；不添加无依据的售电、备用率、线路容量或强制耗尽电池限制。

统一母线守恒：
\[
h+u+G-r+q^{dis}=D+q^{ch}/0.9.
\]
问题一u=0、h=x，二/4-2的h=x，三/4-3的h为最终普通实际购电。无拒收量，r仅弃光。跨日 \(E_{d+1,0}=E_{d,144}^{act}\)，仅问题一初末6000。
\[
\sum(h+u)=\sum(D-G+r)+(19/90)\sum q^{ch}+0.9(E_{\mathrm{end}}-E_{\mathrm{start}}).
\]
不得混用旧qch母线输入定义。有效变量定义下90kWh充入、81kWh放出可保持SOC不变，但母线损耗19kWh；仍要诊断损耗循环，不能把它当作过购保存。

## 4. 实际量费与违约累计：必须先于优化器实现

### 5.1 实际数量与计划修改分开

x为0点策略，\(y^{(k)}\)为允许节点更新后的策略；最终普通实际购电记h，紧急实际购电记u。以每天0点为统计起点：
\[
Q_d^{act}=\sum_t(h_{d,t}+u_{d,t}).
\]
被修改放弃的未来计划量不计入Q，不计其电费，不要求在最终购电表中列出。已执行量不可追溯取消。删除原稿预付合同、持仓、取消批次、按批次旧价计算退款的模型；不需要决定取消哪个合同批次。

### 5.2 违约费保留

在调整时刻a，针对允许修改的未执行区间：
\[
\delta^-_{a,t}=[y^{old}_{t}-y^{new}_{t}]_+,\quad
K_a=0.5p_a\sum_t\delta^-_{a,t},
\]
其中pa为调整当时的实时电价。已发生Ka计入当日最终费用；后来恢复计划不抹掉该违约金。

只需在内部保留足以复核费用的最小调整记录（时刻、减少量、当前价、违约费），或在线累计违约费；无需永久保留每次完整弃用计划。费用审计记录不作为额外购电量，也不是合同持仓。

### 5.3 最终实际费用

令 \(\mathcal E_d\) 为当天已经实际执行的购电事件；\(q_e^{act}\)为实际购买量，ce为该事件执行当时价格，\(\alpha_e\)为题设类别费率：普通计划1、实际调整增购1.5、紧急购电5。事件类别由当时真实执行动作确定，不用最终相对0点的净差额重新分类。
\[
C_d^{act}=
\sum_{e\in\mathcal E_d}\alpha_ec_eq_e^{act}
+\sum_{a\in d}K_a.
\]
实际执行即计费，未来候选策略不是已购买量；不得将每轮预测购电费用重复累计。被放弃的未执行方案电费净额为零。旧稿按预付批次价锁住未来执行价的规则不再使用。

最终数量只需实际轨迹，最终费用还需保留的违约费：相同最终购电量不保证费用相同。已执行增购必须按增购费率核算，不得因删除弃用计划记录而把它改成普通购电。

### 5.4 规划目标

\[
\min_\pi\mathbb E[\sum_{d\in W}C_d^{act}
+V(E_{\mathrm{end}},H_{\mathrm{end}})\mid\mathcal I_{\mathrm{now}}].
\]
实时价只计当前事件；未来部分用因果预测。当前滚动状态保留实际E、历史H、当前有效策略和已发生费用，不再包含预付持仓。未来成本V不计入最终账单。

## 5. 跨日状态和历史信息：后续必须实现

日初策略输入不仅是当天预测，还必须包含昨日实际末SOC、近期负载/光伏/价格、已成熟的预测误差、当前可见跨日预报。历史信息H随新观测更新：
\[
x_d=\mu(E_{d,0},H_{d,0}),\qquad
(E_{d+1,0},H_{d+1,0})=\Phi(\text{当日真实轨迹与新信息}).
\]

优先用多日滚动时域承接剩余电量价值；有限时域末端使用经过去数据校准的未来成本V，不任意设定残值收入。滚动状态保留当前有效策略和已发生违约费用，不维护预付持仓。所有跨午夜预报保留真实有效日期，不能每日截断后全丢弃。

1月1日初始6000仅设一次；1月历史在实际发生后积累，2月1日状态可追溯到1月运行。断点续跑同时恢复SOC、历史窗口、预测器状态、当前策略、违约累计和随机状态，不能仅恢复日期。

不同策略分别连续运行，报告期末SOC与不可行情况；不得为了可比性暗加全年末端回6000硬约束。可另做明确标记的终端敏感性比较。

## 6. 无拒收后的可行性边界

日固定策略购买过多，储能容量/功率又不够时，可能没有合法消纳路径。紧急量只增加供给，不能解决此问题。不得添加外购电弃置项、无成本拒收、无上限电池或损耗烧电机制补洞。

后续应通过多日预测、空间管理和覆盖高光伏/低负载的情景降低风险；有限情景可行不能保证真实运行总可行。出现无法消纳时报告对应日、段、计划、负载、光伏、SOC和约束缺口，停止将该轨迹作为合格结果导出。

## 7. 已确认的附件含义与输出映射

统一采用 \(\Delta t=1/6\) h，区间t=0至143，状态边界0至144。用户已明确：标签00:10表示[00:00,00:10)的平均价格和平均功率；标签24:00表示[23:50,24:00)，不是次日新增交易。

源标签作为区间结束时刻，模型区间为[标签−10分钟,标签)。模板中的错位文字由输出映射层处理，不能因此移动真实数据。原附件不修改；输出副本须附一致的区间说明，模板表头处理属于交付检查，不再是数据含义未决。

附件10分钟功率是区间平均值，故 \(D=L/6,\ G=P^{PV}/6\) kWh；价格直接作为该区间平均单价，不除以6。区间购电费按该平均单价乘该区间购电量离散计算。

附件3的“预报j小时”表示发布时刻a之后 \([a+j-1,a+j)\) 小时的平均功率，不是小时末时点值。“预报1小时”完整覆盖未来首小时，无需第0小时预测、端点补齐或线性插值。当下功率可见，但不能用下一小时实测代替当下观测。

基线将每个小时平均功率均匀用于对应六段，每段预测电量为该小时功率/6；六段之和等于小时预测电量。小时内均匀形状是离散近似，后续可用因果形状预测细化，但须保持小时总量，不能伪称掌握真实10分钟波动。跨午夜保留有效日期。问题一、二不因此获得附件3使用权限。

附件2、4为365×144数据，附件3为1460条小时平均预报。已检查数值结构不等于完成全部质量验证。模板调整表没有发布版本轴，按最终有效执行轨迹映射；若有独立0点计划表仍按题设填写，不与最终购电量相加。原模板不覆盖，输出副本标注准确区间。

## 8. 后续代码阶段验收要求

后续先实现统一物理核、区间平均值映射、当前策略、实际执行记录及违约累计，再实现优化。关键验收：

- 充入750kWh需要母线输入750/0.9，放出750需要电池消耗750/0.9；效率不可重复扣。
- 小时预报分六段后电量和不变，发布时刻/有效区间不混淆。
- 未来方案废弃不计购电量或电费，减少时产生的违约金额不因旧方案被覆盖而消失。
- 不要求永久保存完整中间方案；仅保存最小费用事件以独立复算，或维护可验证累计值。
- 实际增购/紧急动作保留费率类型，不能仅看最终净量重新分类。
- 无拒收过购、损耗循环、信息泄漏和跨日状态仍须检查。
- 输出2月1日至12月31日334天及指定四日；0点计划列与实际总量不重复累计。

当前只改MD，不编写求解代码、不生成工作簿。

## 9. 后续论文阶段要求

后续解释平均价格/功率时间映射、小时内均匀近似、实际有效充放电量与损耗前输入的区别；说明最终实际量与历史违约费为何分开。比较预测、策略调整及跨日价值时控制信息与初始条件，报告实际费用和末SOC。不能将未来成本V计作真实收入，不提前写求解结论。

## 10. 保留的技术待核验与完成状态

不再待核验：源标签含义、小时预报是否时点值、首小时端点、4500kW单位/有效上限、取消策略批次选择。

剩余验证及范围：

| 事项 | 范围 | 影响 |
|---|---|---|
| 小时内形状均匀近似 | 三、4-2、4-3 | 中至高，影响短时储能/紧急量 |
| 当前观测与区间平均值的信息时序 | 二至四，重点四 | 高，防止平均真值提前可见 |
| 冷启动、历史预测和误差校准 | 二至四 | 中至高，影响初月及全年策略 |
| 无拒收过购和损耗循环 | 二至四为重点；循环诊断涵盖一 | 极高，关系可实施性 |
| 跨日V、滚动时域与年末评价 | 二至四 | 高，影响费用比较 |
| 模板表头/单位/汇总映射的落实 | 全部 | 对交付高，不再更改已定数据解释 |

本次完成规则与文档同步，不代表已经求解或完成数值验证。
