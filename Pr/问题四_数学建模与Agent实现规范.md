# C题问题四：数学建模与Agent实现规范

> 2026-09-10按用户确认口径修订；保持问题一的22节组织。当前仅建模，不实现代码、不求解、不撰写论文。按最新解释：最终购电量只统计实际执行量，已发生违约金保留；不使用预付批次。

## 1. 建模目标

使用附件2、3、4分别重做问题二和问题三，输出4-2与4-3。两分支都承认未来实际电价未知，用历史和当前实时电价形成预测；区别是4-2日计划固定，4-3在规定时点可逐笔调整。

## 2. 时间颗粒度与索引规范

统一采用 \(\Delta t=1/6\) h，区间t=0至143，状态边界0至144。用户已明确：标签00:10表示[00:00,00:10)的平均价格和平均功率；标签24:00表示[23:50,24:00)，不是次日新增交易。

源标签作为区间结束时刻，模型区间为[标签−10分钟,标签)。模板中的错位文字由输出映射层处理，不能因此移动真实数据。原附件不修改；输出副本须附一致的区间说明，模板表头处理属于交付检查，不再是数据含义未决。

## 3. 参数与变量体系

物理变量沿用本版前三问。实际价 \(c_{d,t}^{act}\)、未来预测价 \(\widehat c_{d,t\mid a}\) 分开保存；实际购买或违约事件使用发生当时价。4-2使用固定x、紧急u；4-3使用当前策略y、实际执行量h、紧急u与违约累计K。无拒收、合同、预付持仓或原批次价变量。

## 4. 功率向电量转换

附件10分钟功率是区间平均值，故 \(D=L/6,\ G=P^{PV}/6\) kWh；价格直接作为该区间平均单价，不除以6。区间购电费按该平均单价乘该区间购电量离散计算。

附件3的“预报j小时”表示发布时刻a之后 \([a+j-1,a+j)\) 小时的平均功率，不是小时末时点值。“预报1小时”完整覆盖未来首小时，无需第0小时预测、端点补齐或线性插值。当下功率可见，但不能用下一小时实测代替当下观测。

基线将每个小时平均功率均匀用于对应六段，每段预测电量为该小时功率/6；六段之和等于小时预测电量。小时内均匀形状是离散近似，后续可用因果形状预测细化，但须保持小时总量，不能伪称掌握真实10分钟波动。跨午夜保留有效日期。问题一、二不因此获得附件3使用权限。

## 5. 目标函数与费用口径

4-2：
\[
Q_d=\sum_t(x_t+u_t),\quad
C_d=\sum_tc_t^{act}x_t+5\sum_tc_t^{act}u_t.
\]
4-3完整使用问题三第5节：
\[
Q_d=\sum_{e\in\mathcal E_d}q_e^{act},\quad
C_d=\sum_{e\in\mathcal E_d}\alpha_ec_e^{act}q_e^{act}
+\sum_{a\in d}0.5p_a\sum_t\delta^-_{a,t}.
\]
费率α分别为普通1、实际调整增购1.5、紧急5；电价为各事件执行当时价。以当天0点为统计起点，被放弃的未执行方案不计购电量或电费，违约仍按发生时的当前价累计。不得使用原合同批次价格、也不得用最终净差额抹掉违约。

规划目标为多日实际费用条件期望加V(E,H)，未来价格仅使用预测；不得将每轮预测目标重复汇总为账单。

## 6. 能量平衡与购电约束

令4-2的普通实际购电h=x，4-3的h为问题三策略最终实际执行的普通购电量：
\[
h+u+G-r+q^{dis}=D+q^{ch}/0.9,\quad h,u\ge0,\quad0\le r\le G.
\]
不得拒收或抛弃外购电；富余电需使用或保存，4-3另可在允许时点合法取消未交付计划。紧急购电弥补不足，但不能解决满电过购的不可行性。

4-2全天x冻结；4-3仅6/12/18发布节点调整当天剩余计划，不因价格每10分钟更新就无限扩展交易时点。

## 7. 储能状态转移方程

四问均取 \(\eta_c=\eta_d=0.9\)。为准确表示用户确认的“实际4500kW”，本次统一变量定义：

- \(q^{ch}\)：效率损耗后实际存入电池的电量；
- \(q^{dis}\)：效率损耗后实际送达微网的电量；
- 微网充电输入为 \(q^{ch}/0.9\)，电池放电消耗为 \(q^{dis}/0.9\)。

于是：
\[
E_{d,t+1}=E_{d,t}+q^{ch}_{d,t}-q^{dis}_{d,t}/0.9.
\]
充电效率体现在供需平衡的 \(q^{ch}/0.9\) 中，不再对已存入的qch乘一次0.9。旧稿qch定义为母线输入量，旧公式不能与本版变量混用。往返效率仍为0.81。

只有实际能量交换改变E，修改未来计划不直接改变储电量。

## 8. 储能容量与功率约束

\[
1200\le E_{d,t}\le10800,\qquad
P_{\max}^{effective}=5000\times0.9=4500\ {\rm kW},
\]
\[
0\le q^{ch}_{d,t},q^{dis}_{d,t}\le4500/6=750\ {\rm kWh}.
\]
4500的单位是kW，不是W。充电有效存入功率与放电有效输出功率均以4500kW为上限；损耗前的相应输入/消耗功率为有效功率除以0.9，至多5000kW。不得将有效750kWh再乘0.9作为实际充电上限。

额定容量12000不替代允许运行上限10800。储能功率不是外网购电上限；不添加无依据的售电、备用率、线路容量或强制耗尽电池限制。

## 9. 日首日末与跨日状态

两分支分别从2025年1月1日6000kWh开始，实际日末传给次日，均无每日循环约束。不得在2月1日或每个预测发布时点重置电量。

次日策略输入至少包含实际余电、近期负载和光伏、历史价格与当前价格变化、可见跨日光伏预报、历史预测误差。日内未来24小时预报跨午夜部分要保留。多日时域或经历史校准的V承担跨日经济联系，不仅复制一个SOC数值。

## 10. 信息可用性与预测规范

### 10.1 实时价格与历史预测

当前只知道已发生/当前公布的真实价，以及已发布预报。附件4全年真值只能由仿真驱动逐时释放，不能直接进入0点优化器。可用历史同刻、相似日、滞后价格与当前偏差预测剩余日和次日价格；每次得到当前价，更新未来预测，但不修改过去账单。

### 10.2 两分支的权限

4-2允许使用附件3的0点光伏预报制定计划；日内新预报可辅助储能与紧急判断，不可重签普通计划。删除旧稿“4-2不得读取附件3”的限制。

4-3在0/6/12/18获得光伏预报，在允许节点根据最新预测、当前E、负载及价格重新优化剩余计划。价格预测更新频率和普通计划成交权限不是一回事。

### 10.3 禁止信息泄漏

预测特征、训练样本、误差校正都只用当时可得数据；未来已存储的价格/负载不等于已知。全知实际价只可另作标明的不可执行对照，不作主模型。

## 11. 无依据运行限制的清理

删除旧稿没有题设依据的紧急模式限制，包括“紧急时不得充电”“紧急时不得弃光”“必须先把电池用尽”，以及其二元变量和辅助大M约束。紧急量随实际供需残差产生，不随意扩大为独立投机交易。

题面未明确的充放电互斥也不自动加入主模型。仍必须诊断同时充放电及其能量去向：不得将利用损耗循环烧掉已购电解释为“多余电量保存下来”。如主模型最优解依赖此现象，应报告可实施性缺口并核验设备运行机制，不能暗中切换约束后仍声称同一模型。

## 12. 多重最优解与次级目标

主目标为购电实际费用及明确的未来成本估计；不能添加任意弃光罚金、拒收收益、电池残值收入来改变题目目标。

同成本解可用最小充放电吞吐量作次级选择，必须说明是算法选择，不是题面约束。删除“先最小弃光、再最小吞吐”的默认顺序，它可能奖励损耗循环。每次次级求解后重验费用、守恒和物理异常；次级目标不能保证消除所有可实施性问题。

## 13. 完整基线数学模型

4-2复用问题二的固定计划与实际运行模型，4-3复用问题三的策略更新、实际执行计费与违约累计模型。物理约束统一使用有效750kWh上限和本版能量守恒。当前信息节点共享决策，未来价格/负载/预报按时揭示；4-2当天x不变，4-3只能在允许时点改未执行策略。无合同批次或预付持仓。

## 14. 数据预处理要求

原附件和模板只读，排除Excel锁文件。保留真实日期、单位、来源指纹、时间映射和缺失处理记录。已检查的数据结构：

- 附件1：144个00:10至24:00标签，未列日期；价格均正。
- 附件2：2025年365天，每天144段实际负载/光伏。
- 附件3：1460条预报，四次/天，每条24个小时平均功率；空白日期按发布块解析。
- 附件4：365×144条实际电价，当前全部为正，但整表存在不代表提前可见。

只加载本问允许的数据。训练窗口、特征缩放、误差分布、超参数选择均不得读取决策时尚未产生的标签。1月可作历史积累及状态预热，不代表1月未来数据在1月1日已知。

## 15. 后续代码实现要求

复用physics、active_plan、executed_actions、penalty_accumulator与settlement；新增价格预测与历史特征。费用类型区分普通、实际增购、紧急和违约，全部使用相应事件当前价。最终输出可丢弃中间废案，但保留足以核验违约的最小记录。新变量定义使旧模型缓存失效，不能接续旧结果。

## 16. 强制校验与手算用例

继承问题三有效功率手算与量费分离校验。实际普通购电10kWh时价2，应付20；实际增购10kWh时价2，应付30；减少未执行计划10kWh时价2，违约10，该10不计最终购电量。实际购买不能因未来价变化重新计费，未来未执行方案不能按旧方案价锁价。另验未来真值替换不影响当前决策、4-2计划不被日内预测改变、小时平均预报守恒与跨午夜、SOC连续及无拒收过购风险。

## 17. 结果输出要求

输出result4-2.xlsx与result4-3.xlsx，日期和指定四日同前问。最终量费以当天0点起实际执行记录汇总，4-3加已发生违约金；不输出中间废案作为购电量。题设0点计划表可保留初始策略，但不重复计入实际总量。最小费用审计与最终轨迹一致，模板字段落实不改变时间映射。

## 18. 后续论文与评价要求

后续比较价格预测方法时固定交易规则、允许信息、训练范围和初值。报告价格预测误差与真实闭环费用，两者不能互相替代。4-2与4-3的差别包括调整权限；与问题二比较还涉及附件3预报权限，不能把全部费用差归因于算法或价格预测。进行跨日价值、当前价校正、预报更新的消融，报告不可行情况和期末SOC。

## 19. 仍需核验的技术事项

已确认：结束标签平均值、小时平均预报无需第0小时、两向有效4500kW、最终量仅统计实际执行且违约费保留。剩余验证为小时内形状近似（仅使用附件3的分支）、当前观测与区间均值的信息时序、冷启动、历史预测、无拒收过购可行性、同时充放电异常、跨日未来成本与全年末端影响，以及输出模板映射落实。不得恢复合同批次选择、首小时补点或功率侧的旧未决列表。

## 20. 跨问题衔接接口

四问初始6000、两向效率0.9、实际有效功率4500kW统一；只有问题一日循环。4-2继承固定日计划，4-3继承策略修改及实际量费加违约统计，不恢复合同批次。

## 21. Agent推荐执行顺序

后续获准实现时依次进行：验证已确认的区间映射与单位 → 构建统一物理核和信息视图 → 验证手算账单 → 建模 → 连续短窗口因果回测 → 逐段物理/费用/信息核验 → 全年回测 → 模板副本导出。

当前只修改建模与后续要求，不创建求解代码、不运行优化、不生成结果工作簿、不撰写论文结论。

## 22. 最终建模原则

问题四是“未知未来实时价的因果预测＋合法计划权限＋逐笔真实结算＋跨日状态”的统一模型，不是把全年实际价直接代入已知确定性问题。
