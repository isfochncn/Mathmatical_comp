# 工作区整理与防误用记录

> 本文保留清理阶段的历史状态。后续用户要求的三个模型修正及当前auto入口见[问题四三项修正与两周检验](问题四三项修正与两周检验.md)，下文“未修改备用”只描述清理阶段。

日期：2026-09-12。

## 已完成

- 建立[模型优化定稿与验证档案](模型优化定稿与验证档案.md)，保存已确认假设、预测算法参数、实际验证数据和适用问题；未修复事项明确单列。
- 重写运行指南和根README，历史阶段文档增加状态标记，四问规范与假设备忘录指向当前总档案。
- 主模型问题四禁止auto静默回退；历史复现须显式pooled。问题三仍默认report_blend。未修改备用算法或声称问题四已经修复。
- 运行前可用describe检查实际策略，新运行保存run_contract.json。CLI显式传递光伏选择及进度参数。
- tools根目录保留4个通用入口；删除8个重复月度/批量流水线脚本和9个旧结果目录内的导出/探查脚本。13个有独立用途的实验迁至tools/experiments。
- 删除旧out中的临时版本指针，历史实验改用显式输入目录。短程物理账务结果迁至outputs/历史短程验证。
- 从Git取消跟踪54572个重复依赖文件及98个可再生预览/临时文件，新增忽略规则。新导出器直接解析统一Node环境，无需在结果目录中建立依赖链接。
- 清理前源码、旧tools脚本、Pr文档和严格导出器已压缩留档，记录原提交号和逐文件哈希。正式数据与结果的26份工作簿哈希未变。

## 核验

完整测试97项通过；最后调整模型描述后，8项预测与路由测试再次通过。Python语法、文档链接、git diff --check、Node依赖解析和显式模型预检通过。没有重跑主模型，也没有改写历史测试结果。

## 尚未完成的物理清理

自动审批审查先后拦截了带路径边界检查的批量删除、单个依赖junction删除，返回 `blocked by policy`，未给出进一步原因。停止这些删除后，改用仅修改Git索引的取消跟踪；外部统一运行环境保持不变。

清理清单中仍有95个可再生预览/探查文件、7个依赖junction、8个临时或缓存目录位于磁盘；目录可能为空或包含前述文件，因此这些数量不能相加视为互不重叠文件数。Python缓存之后也可能自动生成。上述物理残留没有被伪报为已删除，均不再作为运行入口；不能宣称工作区物理上已经完全清空。

详细清单和核验记录保存在 `outputs/源码留档/本轮清理清单.json`、`本轮整理核验.json`。Git取消跟踪操作已暂存，其他源代码/文档修改待用户查看，本轮未创建提交。
